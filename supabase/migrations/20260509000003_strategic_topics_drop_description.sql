-- ============================================================================
-- strategic_topics — удаление колонки description
-- ============================================================================
-- На практике synopsis (1–2 предл) и description (4–7 предл) дублировали друг
-- друга — оба описывали ситуацию темы. Решение: оставить только synopsis,
-- расширив его до 1–4 предложений как «единственное описательное поле».
--
-- Контент существующих description НЕ переносится автоматически в synopsis —
-- для seed-тем это сделано через отдельный UPDATE из текста аналитического
-- отчёта (см. 20260509000004_strategic_topics_quotes_from_report.sql).
-- ============================================================================

alter table strategic_topics drop column description;

notify pgrst, 'reload schema';
